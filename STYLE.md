# Визуальный регламент Jol Ustazy

Это не пожелания, а обязательные правила. Внешний вид сайта — часть доверия к нему:
человек готовится по нему к настоящему экзамену, и пестрота читается как несерьёзность.

**Главный принцип: работу делают типографика и воздух, а не украшения.**
Если хочется что-то добавить — сначала уберите лишнее.

---

## 1. Цвет

Все цвета живут **только** в CSS-переменных в начале `index.html`. Ни одного шестнадцатеричного
кода в разметке, в стилях компонентов или в JS. Нужен новый оттенок — сначала добавьте токен.

### Тёмная тема (основная, показывается по умолчанию)

```
--bg:#0A120E         холст, почти чёрный зелёный
--surface:#111F18    карточки
--surface-2:#1A2B22  вложенные поверхности, поля
--ink:#EDF3EE        основной текст
--ink-2:#9BB5A6      вторичный текст
--ink-3:#6E8A7B      подписи, третьестепенное
--line:#1E3228       границы
--line-2:#2C4A3B     границы поярче, обводка неактивного
--accent:#4EA37B     интерактив: ссылки, активные состояния
--accent-deep:#1F6B4A заливка главной кнопки
--accent-soft:#12291F подложка выбранного
--on-accent:#F0F5F1  текст на заливке
--moss:#A9BC7E       второй акцент: только мелкие надрубрики
--ok:#57BC8B         верный ответ
--bad:#D8785F        ошибка (глиняный, тёплый)
--warn:#C79A4B       предупреждение
--r:12px --r-sm:9px  радиусы, других нет
```

### Светлая тема

Включается **только** явным переключателем, `:root[data-theme="light"]`. Никаких
`prefers-color-scheme`: по умолчанию сайт тёмный у всех.

```
--bg:#F2F0E9  --surface:#FBFAF6  --surface-2:#EAE8DF
--ink:#152019 --ink-2:#4A5A50    --ink-3:#78887E
--line:#DEDBD0 --line-2:#C9C5B7
--accent:#1F6B4A --accent-soft:#E3EEE7 --on-accent:#FBFAF6 --moss:#6E7C46
--ok:#1D7A4C --bad:#B0503B --warn:#8E6A26
```

### Правила обращения с цветом

- **Акцент — редкость.** Главная кнопка, активное состояние, ссылка. Всё. Если на экране
  больше трёх зелёных пятен, вы переборщили.
- **Мох `--moss`** — исключительно мелкие надрубрики капителью. Никаких заливок им.
- **Глиняный `--bad`** — только ошибки. Это единственный незелёный цвет в палитре, он
  работает именно потому, что редкий.
- **Цвета дорожных знаков не трогать никогда.** Красный круг и синий квадрат — нормативные.
  Перекрашивать их под тему нельзя, это уже не оформление, а искажение содержания.

---

## 2. Шрифты

```
--serif:"Source Serif 4","Iowan Old Style",Georgia,serif
--sans:"Source Sans 3",-apple-system,"Segoe UI",system-ui,sans-serif
--mono:"IBM Plex Mono",ui-monospace,Menlo,monospace
```

| Что | Чем | Как |
|---|---|---|
| Заголовки, текст вопроса, названия режимов | serif | вес 600, трекинг −0.012em, интерлиньяж 1.18 |
| Основной текст, варианты ответов, кнопки | sans | 17px, интерлиньяж 1.62 |
| Надрубрики, счётчики, таймер, цифры статистики | mono | 10–11px, ТРЕКИНГ 0.18–0.22em, ПРОПИСНЫЕ |

- Крупный заголовок: `clamp(38px,8.5vw,58px)` на телефоне, `clamp(52px,4.6vw,66px)` на ПК.
- Текст вопроса: serif 22px, на телефоне 19.5px, на ПК 24px. Вопрос — это главное на экране.
- **Третьего шрифта нет.** Никаких Google Fonts «для разнообразия».
- Кнопки — обычным регистром, sans, вес 600. Капслок только у mono-меток.

---

## 3. Ритм и сетка

Шаг отступов: **8 · 12 · 18 · 24 · 32 · 44 · 56 · 76 · 88**. Промежуточных значений нет.

- Ширина контейнера: 680px на телефоне, **1120px от 900px ширины экрана**.
- Главная на ПК: две колонки `1.1fr .9fr`, промежуток 64px. Действия слева, справочное справа.
- Обёртки колонок на телефоне схлопываются через `display:contents` — порядок блоков
  обязан остаться прежним.
- **Длинный текст никогда не растягивается на всю ширину.** Проза, разбор, итог — не шире
  78 символов, по центру. Читать строку в 1100px невозможно.
- Карточка вопроса на ПК: схема слева фиксированной колонкой 340–420px во всю высоту
  карточки, текст и варианты справа.

---

## 4. Компоненты

- **Границы** 1px, `--line`. Толще не бывает.
- **Радиусы** только `--r` (12px) и `--r-sm` (9px).
- **Тень** одна на весь проект, `--shadow`, и только у карточек. Больше нигде.
- **Иконки** линейные, 22px, обводка 2, `currentColor`. Заливок нет.
- **Варианты ответа** в покое прозрачные, с тонкой обводкой `--line-2`. Подсвечиваются
  только при наведении и выборе.
- **Кнопки** на ПК не растягиваются во всю ширину: `min-width:190px`, прижаты вправо.
  На телефоне — во всю ширину, это нормально.

---

## 5. Поведение

- **Наведение только там, где есть мышь:** все `:hover` обязаны быть внутри
  `@media (hover:hover)`. На сенсорных экранах наведение залипает после тапа.
- **Цели нажатия** не меньше 48px. Кнопки, варианты, темы.
- **Безопасные зоны iPhone:** `env(safe-area-inset-*)` на шапке, полях и подвале.
- **Отклик на нажатие** — лёгкое `scale(.994)` или снижение прозрачности. Никаких
  подпрыгиваний и пружин.
- Переходы 0.15–0.2s. Анимаций появления, параллакса и прочего движения нет.
- `@media (prefers-reduced-motion:reduce)` отключает переходы. Не удалять.

---

## 6. Чего не делать никогда

- Градиенты. Любые.
- Неон, свечение, `box-shadow` цветом акцента.
- Стеклянные панели, размытие фона (кроме уже существующего в липкой шапке).
- Фиолетовый и синий — штампы «искусственного интеллекта», к дорожной теме отношения не имеют.
- Эмодзи в интерфейсе.
- Больше одного акцентного цвета на экран.
- Иконки ради иконок. Если рядом с надписью иконка ничего не добавляет — её не должно быть.
- Карточки внутри карточек внутри карточек.
- Скруглённые «пузыри» с радиусом больше 12px.
- Центрированный по горизонтали длинный текст.
- Заглавные буквы у обычного текста и заголовков.
- Собственные цвета мимо токенов.

---

## 7. Как проверять

После любой правки внешнего вида:

1. **393×852** — iPhone 16. Ничего не вылезает, горизонтальной прокрутки нет.
2. **1440×900** — ПК. Контент не жмётся в узкую колонку, справа не пустует.
3. Обе темы: тёмная по умолчанию и светлая по переключателю.
4. Контраст: основной текст к фону не ниже 12:1, вторичный не ниже 7:1, обводка
   верного и неверного ответа к карточке не ниже 4.5:1.
5. Экран вопроса со схемой и без неё — раскладки разные.

Проверять на локальной сборке или на опубликованном сайте: артефакт встроенный
браузер не открывает.

---

## 8. Настроение

Спокойный, уверенный, редакторский. Ближе к хорошо свёрстанному справочнику, чем
к мобильной игре. Дорого выглядит не тот интерфейс, где много всего, а тот, где
каждый элемент оправдан и между ними есть воздух.

Ориентир по ощущению: строгая книжная вёрстка, тёмный зелёный как цвет дорожного
указателя, ни одной случайной детали.
